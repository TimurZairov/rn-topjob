import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ProfileFieldScreen = () => {
  return (
    <View>
      <Text>ProfileFieldScreen</Text>
    </View>
  );
};

export default ProfileFieldScreen;

const styles = StyleSheet.create({});
